import React, { FC } from 'react';

import ProfileRoutes from '../../containers/AppRoutes/ProfileRoutes';

const Profile: FC = () => {
  return (
    <>
      sdkfmhd
      <ProfileRoutes />
    </>
  );
};

export default Profile;
